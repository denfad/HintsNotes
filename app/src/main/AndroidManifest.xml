<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.denfad.hintsnotes">

    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/blyaaa"
        android:label="@string/app_name"
        android:roundIcon="@drawable/blyaaa"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight">
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".HintListActivity">

        </activity>
        <activity android:name=".AddingActivity">

        </activity>

        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LecturesListActivity">

        </activity>
        <activity android:name=".SettingsActivity" />
    </application>

</manifest>